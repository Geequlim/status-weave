# Status Weave：产品与开发计划

## 1. 项目定位

**Status Weave** 是一个面向 Cinnamon 面板的可扩展系统状态监控 applet。

它的目标不是做一个塞满图表的系统监视器，而是提供一套稳定的“采集一次、按需展示”的基础设施：用户可以把 CPU、内存、温度、风扇、显卡和网络等状态，自由组合到一个或多个 Cinnamon 面板中。

项目从一开始就按可公开发布设计，但第一阶段优先解决自己的真实使用场景：新一代混合显卡笔记本、Cinnamon、双外接显示器、日常需要紧凑而可靠的硬件状态提示。

## 2. 已确定的产品原则

1. **Cinnamon 是唯一目标桌面。** 不以 GNOME 为备选方案或兼容目标。
2. **稳定和无掉帧优先。** 插件不能阻塞 Cinnamon 主线程，也不能因单个硬件接口异常拖垮面板。
3. **面板优先。** 默认是紧凑的一行状态信息，不做大型图表仪表盘。
4. **多实例是一等能力。** 用户可以在顶部或底部水平面板添加多个 Status Weave 实例；每个实例独立决定显示内容和顺序。
5. **采集与展示分离。** 设备查询、指标建模、格式化和面板布局不能耦合。
6. **按设备批量采样。** 一次 NVIDIA 查询应同时取得温度、利用率、显存等数据，不能为每个显示字段重复访问硬件。
7. **可退化。** 不支持或暂时读不到的传感器应被标为不可用，不显示伪造数据，也不影响其他指标。
8. **长期维护优先。** 使用 TypeScript 开发、严格类型检查、清晰的模块边界和可测试的数据模型；运行时不依赖 Node.js 或 TypeScript。

## 3. 产品范围

### 第一版核心能力

- CPU：总利用率、温度。
- 内存：已用量、总量、使用率。
- 风扇：所有已发现风扇的转速。
- GPU：集显与独显的利用率、温度；独显可追加显存使用量。
- 网络：所有可用网卡的下载与上传实时速率。
- 面板：紧凑文本、状态色、工具提示、可配置显示顺序。
- 多实例：每个面板实例独立选择指标、格式和告警阈值。

### 明确不在第一版做的内容

- 面板内折线图、历史曲线和复杂仪表盘。
- 远程监控、云同步、账户或遥测上报。
- 任何需要 root 权限的写操作。
- 为了支持少数传感器而引入常驻 Python 后端。

### 后续可选方向

- 电池健康度、充放电功率与充电阈值状态。
- 磁盘温度、空间、读写速率。
- CPU/GPU 功耗、频率与性能模式。
- VPN、蓝牙、扩展坞和自定义脚本指标。
- 可选的详情弹窗；仍以轻量和不阻塞为前提。

## 4. 总体架构

```text
┌──────────────────────────────────────────────────────────┐
│ Cinnamon Panel Instances                                  │
│  A：CPU / RAM / 网络    B：独显 / 风扇    C：自定义组合    │
└───────────────────────────┬──────────────────────────────┘
                            │ 订阅展示字段
┌───────────────────────────▼──────────────────────────────┐
│ Presentation Layer                                         │
│ 每实例配置、排序、格式、颜色、阈值、工具提示               │
└───────────────────────────┬──────────────────────────────┘
                            │ 读取缓存快照
┌───────────────────────────▼──────────────────────────────┐
│ Telemetry Service（进程内共享）                            │
│ 设备发现、采样调度、缓存、超时、错误隔离、订阅通知         │
└───────────────────────────┬──────────────────────────────┘
                            │ 一次采样一个设备快照
┌───────────────┬───────────┼──────────────┬────────────────┐
│ CPU / Memory  │ hwmon     │ GPU Providers│ Network         │
│ /proc         │ /sys      │ AMD / NVIDIA │ /proc / sysfs   │
└───────────────┴───────────┴──────────────┴────────────────┘
```

### 4.1 设备提供者（Provider）

Provider 是唯一直接接触系统接口的层。每个 Provider 一次采样，返回一个设备快照。

示例：NVIDIA Provider 一次异步查询，返回：

```text
GPU 快照：名称、利用率、温度、显存已用、显存总量、功耗、时间戳、可用性
```

上层展示的 `gpu.discrete.temperature`、`gpu.discrete.usage` 与 `gpu.discrete.memory.used` 都只是同一份快照的字段投影，不会分别触发硬件查询。

### 4.2 指标目录（Metric Catalog）

指标不是“查询动作”，而是对快照字段的稳定描述。每项指标应定义：

- 稳定 ID，例如 `cpu.usage`、`memory.used`、`gpu.discrete.temperature`。
- 数据类型与单位，例如百分比、摄氏度、字节、RPM、字节每秒。
- 关联 Provider 和快照字段。
- 建议刷新频率与可用性条件。
- 默认格式化规则。
- 默认警告与危险阈值。

### 4.3 采样服务（Telemetry Service）

- 全部 I/O 必须异步执行，禁止在 Cinnamon 主线程进行阻塞读写或同步子进程调用。
- 多个面板实例共享 Provider 缓存。
- 服务按“所有订阅者需要的最高频率”调度同一 Provider；不因多实例重复采样。
- Provider 设定超时、失败退避和最后成功快照。
- 单个 Provider 失败只影响其相关指标。
- 设备热插拔、网卡消失或驱动暂时不可用时，可重新发现并恢复。

### 4.4 面板实例（Panel Instance）

每个实例只拥有展示配置，不拥有硬件采样状态。独立配置包括：

- 显示哪些指标。
- 指标顺序和分组。
- 每项指标的紧凑格式。
- 警告/危险阈值及对应颜色策略。
- 刷新偏好与工具提示开关。
- 顶部或底部水平面板的布局适配；不支持左右垂直面板。

默认配置应给出一个全指标实例；高级用户可拆成“顶部常驻概览”和“底部 GPU/风扇详情”等多个实例。

## 5. 技术选型

### 5.1 语言与运行时

- 源码：采用用户指定的最新 TypeScript 7 工具链，并开启严格类型检查。
- 运行时：Cinnamon CJS 加载编译后的 JavaScript。
- 不在最终安装包中要求 Node.js、TypeScript 或 Python 常驻运行。
- 编译目标需以 Cinnamon CJS 的实际 JavaScript/SpiderMonkey 兼容性为准，而非 Node.js 的最新语法能力。

### 5.2 类型声明

- 使用 `@ci-types/cjs` 作为 CJS、GLib、Gio 和 GObject Introspection 库的基础类型来源。
- 为实际使用的 Cinnamon applet API 维护一层本地、窄范围的 TypeScript 声明适配。
- 不让业务代码直接依赖不完整或未经验证的 Cinnamon 内部声明。
- 在项目启动阶段做一个最小的“类型与运行时兼容性验证”样例，确认 imports、设置 API、面板 API、异步子进程和文件读取均可用。

### 5.3 数据来源策略

- CPU、内存、网络：优先 `/proc`、`/sys` 和经验证的 GLib/GTop 能力。
- 温度与风扇：`/sys/class/hwmon`，配合设备发现和标签映射。
- AMD/集显：优先公开的 DRM/sysfs 状态接口；无数据时优雅不可用。
- NVIDIA/独显：优先驱动公开接口；必要时使用带固定 argv、异步执行、可超时的只读查询命令。
- 所有外部命令必须避免 shell 拼接，使用参数数组，并对输出做严格解析。

### 5.4 配置策略

- 使用 Cinnamon 原生的 `settings-schema.json` 为基础设置生成界面。
- 每个 applet 实例有自己的展示配置。
- 全局服务配置仅保存真正全局的诊断和 Provider 行为；不把面板布局塞进全局配置。
- 初期优先提供直观的勾选、排序、阈值和格式设置；复杂的布局编辑器留到核心稳定以后。

## 6. 推荐的仓库结构

```text
status-weave/
├─ src/
│  ├─ applet/                 # Cinnamon applet 入口与实例生命周期
│  ├─ telemetry/
│  │  ├─ providers/           # CPU、hwmon、NVIDIA、AMD、网络提供者
│  │  ├─ snapshots/           # 设备快照类型
│  │  ├─ metrics/             # 指标目录与字段投影
│  │  └─ scheduler/           # 缓存、订阅、调度、错误隔离
│  ├─ presentation/           # 格式化、阈值、状态色、工具提示
│  ├─ settings/               # 配置映射与实例配置模型
│  ├─ platform/               # CJS / Cinnamon 窄适配层
│  └─ types/                  # 本地声明补充
├─ tests/
│  ├─ unit/                   # 纯 TS 模型、格式化、解析、调度测试
│  ├─ fixtures/               # /proc、sysfs、驱动输出样本
│  └─ integration/            # 构建产物和 Cinnamon 安装包检查
├─ packaging/
│  └─ spice/                  # 输出为 Cinnamon Spices 所需目录结构
├─ scripts/                   # 构建、打包、安装、验证辅助脚本
├─ docs/
├─ README.md
└─ LICENSE
```

源码仓库保留完整 TypeScript 工程；公开发布时生成单独的 Spices 安装包，不把开发依赖、测试夹具或私有机器路径带进发布目录。

## 7. 分阶段开发计划

本节描述完整阶段边界；实际开发顺序、工期和每阶段质量门禁见 [执行路线图](docs/execution-plan.md)。

### 阶段 0：发布与兼容策略确认

**目标：** 确定项目从个人使用走向公开发布的边界。

- 确定 GitHub 仓库、许可证、作者标识和 UUID。
- UUID 建议采用 `status-weave@<作者标识>`；显示名称为 `Status Weave`。
- 确定首批正式支持的 Cinnamon 版本和 Linux Mint 版本。
- 阅读 Cinnamon Spices 的目录、验证与提交要求。
- 明确“支持硬件”与“尽力探测硬件”的区别，避免承诺无法测试的设备。

**验收：** 仓库元信息和兼容性声明可公开阅读，UUID 不冲突。

### 阶段 1：项目骨架与最小 applet

**目标：** 证明 TypeScript → Cinnamon JavaScript 的完整链路可靠。

- 初始化 TypeScript 7、格式化、lint、类型检查和测试命令。
- 建立 CJS/Cinnamon 类型适配层。
- 建立构建产物目录和本机开发安装命令。
- 编写最小多实例 applet：能添加两次，每个实例显示不同静态文本和独立设置。
- 验证 applet 移除时定时器、信号和订阅均正确释放。

**验收：** 多实例可安装、可卸载、可重载；构建失败不会留下半成品安装目录。

### 阶段 2：遥测核心

**目标：** 完成可测试的“采样一次、多个展示指标复用”模型。

- 实现 Provider、Snapshot、Metric、Subscription 和 Scheduler 接口。
- 实现缓存、超时、错误状态、最后成功值和恢复逻辑。
- 实现模拟 Provider，以便没有真实硬件时测试面板和阈值。
- 验证多个实例订阅同一 GPU 字段时只触发一次查询。

**验收：** 单元测试可证明查询去重、失败隔离、刷新策略和订阅生命周期正确。

### 阶段 3：核心数据提供者

**目标：** 覆盖最常用、最通用的基础指标。

1. CPU 与内存 Provider。
2. 网络 Provider：接口发现、上下行速率、接口消失后的恢复。
3. hwmon Provider：温度和风扇发现、标签映射、不可用退化。
4. NVIDIA Provider：温度、利用率、显存；命令或接口异常不影响面板。
5. AMD/集显 Provider：支持可公开、可验证的利用率与温度来源。

**验收：** 每个 Provider 都有录制夹具、解析测试和真实机器上的对照测试；无硬件或驱动时不报错。

### 阶段 4：面板呈现与配置

**目标：** 让用户能舒适地组合和阅读指标。

具体交互形态以 [产品形态与交互原则](docs/product-shape.md) 为准。

- 实现紧凑状态项目、分隔符、状态色与工具提示。
- 实现实例级指标开关、排序、格式和阈值。
- 显式声明只支持顶部与底部水平面板。
- 提供默认预设：概览、GPU/风扇、网络。
- 先保证简单设置的可靠性，再考虑高级布局编辑。

**验收：** 同一指标可以在不同实例以不同格式显示；配置变更立即生效且不重新初始化全部 Provider。

### 阶段 5：稳定性、性能与兼容性

**目标：** 达到“长期常驻也不影响桌面”的质量标准。

- 检查所有 I/O 为异步；没有阻塞式子进程和无限等待。
- 检查所有 timer、signal、subprocess 在 applet 移除时释放。
- 进行多实例、睡眠唤醒、网卡变化、显卡模式切换和传感器消失测试。
- 测量常驻 CPU、内存和查询频率；避免不必要的轮询。
- 在声明支持的 Cinnamon 版本上做安装和回归测试。

**验收：** 出现单项采样错误时面板持续可用；长期运行无日志刷屏、无资源泄漏、无明显桌面卡顿。

### 阶段 6：文档与公开发布准备

**目标：** 让陌生用户能够安全安装、理解并反馈问题。

- 完善 README：功能、截图、支持范围、依赖、安装、配置、故障排查。
- 写明哪些数据依赖 NVIDIA 驱动、lm-sensors、特定内核接口或厂商硬件。
- 提供问题反馈模板：Cinnamon 版本、发行版、硬件、驱动、日志摘要。
- 补充许可证、隐私说明和安全边界：不上传数据、不需要 root、不执行用户输入的 shell。
- 建立发布说明和版本号规则。

**验收：** 新用户可以不读源码完成安装、配置与问题报告。

### 阶段 7：提交 Cinnamon Spices

**目标：** 进入官方 Cinnamon 分发渠道。

- 按官方目录规范生成 Spices 包：元信息、截图、README、安装目录和编译后的 applet 文件。
- 使用官方 `validate-spice` 和本机安装测试流程验证。
- 在官方 `cinnamon-spices-applets` 仓库提交新 applet Pull Request。
- PR 中说明功能、支持范围、外部依赖、异步/安全设计和实际测试环境。
- 根据审核意见修改；合并后通过 Cinnamon Spices 网站和系统设置分发更新。

**验收：** PR 合并，用户可在 Cinnamon 系统设置中发现、安装和更新 Status Weave。

## 8. 发布路径与维护模式

### 开发期

- 先在自己的 GitHub 仓库维护源代码和 issue。
- 本机通过开发安装流程测试，不直接修改系统内置 applet。
- 所有发布包从干净构建生成。

### 公开期

- 官方 Cinnamon Spices 仓库存放可安装的发布内容。
- 自己的仓库继续作为 TypeScript 源码、测试、CI 和设计讨论的主仓库。
- 每次发布保留变更说明与兼容性说明。

### Cinnamon Spices 规则摘要

- 官方 applet 集中在 `linuxmint/cinnamon-spices-applets` 仓库。
- 新 applet 和后续更新通过 Pull Request 提交。
- 维护团队审核结构、安全性和对桌面环境的潜在影响；作者仍负责自己的 applet。
- 合并后，Spices 网站和 Cinnamon 系统设置自动提供安装与更新。
- 没有公开审核时限；首个 PR 应准备完整的说明、截图、验证结果和兼容性信息。

参考：

- [Cinnamon Spices Applets 官方仓库与发布流程](https://github.com/linuxmint/cinnamon-spices-applets)
- [官方仓库 README：目录规范、验证、开发和 PR 规则](https://github.com/linuxmint/cinnamon-spices-applets/blob/master/README.md)
- [CJS TypeScript 声明包说明](https://npm.io/package/%40ci-types/cjs)

## 9. 首个 GitHub 仓库的建议内容

创建仓库时，建议第一批提交只包含：

- `README.md`：项目定位、当前阶段、设计原则。
- `LICENSE`。
- `docs/development-plan.md`：本文档。
- `docs/architecture.md`：后续从本文档拆出的接口设计。
- TypeScript 工程骨架与最小 applet。
- CI：类型检查、格式化、单元测试、构建和发布包结构检查。

不要在首个提交就急于完成全部硬件支持。先证明多实例、共享采样、TypeScript 构建与 Cinnamon 生命周期正确，再逐个增加 Provider，才能把 Status Weave 做成可长期维护的工具。
